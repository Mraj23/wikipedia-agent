alpha_sweep: [0.05, 0.1, 0.2]
token_budget_sweep: [32, 64, 96]
group_size: 8
learning_rate: 1.0e-5
epochs: 1
note: |
  Stub. Implement after RLTF-SD data generation works (plan §11).
  Pattern: GRPO with reward = max(0, 1 - alpha * reasoning_tokens / budget)
  if correct else 0. Use Tinker's importance_sampling loss.
